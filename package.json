{
  "devDependencies": {
    "elm": "^0.19.0-no-deps",
    "elm-format": "^0.8.2",
    "elm-live": "^4.0.1"
  },
  "scripts": {
    "dev": "elm-live src/Main.elm -u -- --output=index.js",
    "build": "elm make src/Main.elm --output=index.js"
  }
}